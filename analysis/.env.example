# 复制成 analysis/.env 后填写。.env 已在 .gitignore 里，不会被提交。

# ---- 必填：任选一家 LLM（填哪个就用哪个，多个都填按此文件顺序优先）----
ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# GOOGLE_API_KEY=

# ---- 强烈建议：新闻数据 ----
# yfinance 的新闻接口 2026-08 起返回空，没有这个 key 的话「新闻分析师」和
# 「情绪分析师」基本无料可分析。Alpha Vantage 免费档（25 次/天）即可：
# https://www.alphavantage.co/support/#api-key
# ALPHA_VANTAGE_API_KEY=

# ---- 可选：宏观数据（FRED，免费）----
# https://fred.stlouisfed.org/docs/api/api_key.html
# FRED_API_KEY=

# ---- 模型选型（决定每次跑多少钱，成本对比见 README）----
# ⚠️ deep 和 quick 必须同一家（框架只有一个 llm_provider）。
# 14 个节点里只有 2 个用 deep，其余全用 quick —— quick 决定成本，deep 换好模型几乎不花钱。
#
# 方案 A：便宜 + 关键决策不将就（推荐，约 $0.27/次）
# TRADINGAGENTS_QUICK_THINK_LLM=claude-haiku-4-5
# TRADINGAGENTS_DEEP_THINK_LLM=claude-opus-5
#
# 方案 B：DeepSeek（约 $0.06/次）—— 走 OpenAI 兼容接口，key 在 platform.deepseek.com 申请
# OPENAI_API_KEY=sk-...                      # ← 填 DeepSeek 的 key，不是 OpenAI 的
# TRADINGAGENTS_LLM_PROVIDER=openai
# TRADINGAGENTS_LLM_BACKEND_URL=https://api.deepseek.com
# TRADINGAGENTS_QUICK_THINK_LLM=deepseek-v4-pro
# TRADINGAGENTS_DEEP_THINK_LLM=deepseek-v4-pro
#
# 想更便宜（约 $0.02/次）可把 quick 换成 deepseek-v4-flash，但 DeepSeek 官方只在 V4-Pro
# 上明确写了支持工具调用——4 位分析师全靠工具调用取数据，flash 若不支持就会空转出废报告。
# 先用 Pro 跑通，再单独试 flash，并检查报告里分析师是否真的引用了行情数据。
#
# 方案 C：不计成本求质量（约 $0.82/次）
# TRADINGAGENTS_QUICK_THINK_LLM=claude-opus-5
# TRADINGAGENTS_DEEP_THINK_LLM=claude-opus-5
# 报告语言，默认中文（智能体内部辩论仍用英文，官方称这样推理质量更好）
# TRADINGAGENTS_OUTPUT_LANGUAGE=Chinese
